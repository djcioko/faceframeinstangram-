# Face Frame Effect 🎥

Acest proiect replică efectul de **Face Frame** din Instagram folosind Python.

## Tehnologii folosite:
* **Streamlit** - Pentru interfața web.
* **MediaPipe** - Pentru detecția feței.
* **OpenCV** - Pentru procesarea grafică.

## Cum să-l rulezi local:
1. Clonează repository-ul.
2. Instalează dependențele: `pip install -r requirements.txt`.
3. Rulează aplicația: `streamlit run app.py`.
